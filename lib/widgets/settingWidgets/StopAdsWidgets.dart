import 'package:flutter/material.dart';

class StopAdsWidget extends StatelessWidget {
  const StopAdsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Stop Ads"),
      ),
      body: Center(
        child: Text("Coming Soon!"),
      ),
    );
  }

}